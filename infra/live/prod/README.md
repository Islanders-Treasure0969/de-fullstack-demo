# prod (placeholder)

Future home for production environment definitions when the project graduates from LocalStack/Garage to real AWS S3.
The directory layout mirrors `dev/` so modules are reused unchanged.

Phase 6 will populate this.
