# dbt + Iceberg client deps for the transform/ project.
# Pinned to dbt-core 1.10 (Phase 4 will switch to dbt Fusion).
# See ADR-0005 (Fusion) and ADR-0010 (Iceberg via Lakekeeper).

dbt-core>=1.10,<1.11
dbt-duckdb>=1.10,<1.11

# Phase 2: PyIceberg for Iceberg writes against the Lakekeeper REST catalog.
# `s3fs` extras enable Garage S3 IO; `pyarrow` is shared with dbt-duckdb.
pyiceberg[s3fs,pyarrow]>=0.10,<0.12

# Data quality (Phase 4)
# soda-core-duckdb>=3.5
